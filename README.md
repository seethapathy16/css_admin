# css_administration

create a folder as css admin

inside that css admin folder, create 2 folder one as frontend and another as backend

adding some module in each seperate folder

in backend:
      open backend folder and click ctrl+`
      after opened the cmd prompt, set the path 
      if complete to set the path, paste this command "npm init -y"
      some modules will be added in the folder
      
